/**
 * Settings button component - currently hidden as dark mode is enforced.
 * Theme toggle code kept for potential future use.
 */
export default function SettingsButton() {
  // Dark mode is currently enforced - settings button hidden
  // To re-enable theme toggle, restore the original implementation from git history
  return null;
}
